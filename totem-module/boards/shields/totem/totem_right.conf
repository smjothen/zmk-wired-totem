CONFIG_UART_CONSOLE=n

# Enable PIO UART driver for swapped TX/RX pins
CONFIG_UART_RPI_PICO_PIO=y

# Disable double-tap bootloader — it breaks PIO UART on the wired split peripheral
CONFIG_ZMK_DBL_TAP_BOOTLOADER=n
